-- Создание GUI объектов
local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local UICorner = Instance.new("UICorner")
local CloseBtn = Instance.new("TextButton")
local Title = Instance.new("TextLabel")
local ActionBtn = Instance.new("TextButton")
local UICornerBtn = Instance.new("UICorner")

-- Настройка ScreenGui (чтобы не пропадал при смерти)
ScreenGui.Parent = game:GetService("CoreGui")
ScreenGui.Name = "XenoMenu_Black"
ScreenGui.ResetOnSpawn = false

-- Главная рамка (Центр, Черная, Закругленная)
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.Position = UDim2.new(0.5, -100, 0.5, -75)
MainFrame.Size = UDim2.new(0, 200, 0, 150)
MainFrame.Active = true
MainFrame.Draggable = true -- Работает на ПК

-- Закругление углов
UICorner.CornerRadius = UDim.new(0, 12)
UICorner.Parent = MainFrame

-- Заголовок
Title.Parent = MainFrame
Title.BackgroundTransparency = 1
Title.Position = UDim2.new(0, 15, 0, 5)
Title.Size = UDim2.new(0, 120, 0, 30)
Title.Font = Enum.Font.GothamBold
Title.Text = "XENO MENU"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 14
Title.TextXAlignment = Enum.TextXAlignment.Left

-- Кнопка закрытия (Крестик)
CloseBtn.Name = "CloseBtn"
CloseBtn.Parent = MainFrame
CloseBtn.BackgroundColor3 = Color3.fromRGB(255, 60, 60)
CloseBtn.Position = UDim2.new(1, -30, 0, 8)
CloseBtn.Size = UDim2.new(0, 22, 0, 22)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 14

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(1, 0)
CloseCorner.Parent = CloseBtn

-- Пример кнопки выбора внутри
ActionBtn.Parent = MainFrame
ActionBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
ActionBtn.Position = UDim2.new(0.1, 0, 0.4, 0)
ActionBtn.Size = UDim2.new(0.8, 0, 0, 35)
ActionBtn.Font = Enum.Font.Gotham
ActionBtn.Text = "Включить чит"
ActionBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ActionBtn.TextSize = 14

UICornerBtn.CornerRadius = UDim.new(0, 6)
UICornerBtn.Parent = ActionBtn

-- Логика закрытия (Удаляет меню полностью)
CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- Скрипт для перетаскивания пальцем (для телефонов)
local UserInputService = game:GetService("UserInputService")
local dragging, dragInput, dragStart, startPos

MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = false
    end
end)
